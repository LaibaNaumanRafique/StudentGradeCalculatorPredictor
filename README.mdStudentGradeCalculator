/*AUTHOR: lAIBA NAUMAN MAHMOOD RAFIQUE
DATE:26 SEPTEMBER 2025
DESCRIPTION : THIS PROGRAM ASKS FOR STUDENTS NAME AND THEN CALCULATE GRADES BASED ON GIVEN CONDITIONS
 */
package com.laibA; //package is a folder where all java files are saved .


import java.util.Scanner;// Scanner used for taking input from user.

public class StudentGradeCalculator { //class name and file name must be matched otherwise cause error.
    public static void main(String[] args) { //main is the main entry point where program begins.

Scanner sc = new Scanner(System.in);
        System.out.println("Enter Student name: ");
        String name = sc.nextLine();
        System.out.println("Enter Student marks : ");
        int marks = sc.nextInt();
        if (marks < 0 || marks > 100)
            System.out.println("invalid marks ! please try again:");
        else if (marks >= 90 && marks <= 100)
            System.out.println("A");
        else if (marks >= 80 && marks<90)
            System.out.println("B");
        else if (marks >= 70)
            System.out.println("C");
        else if (marks >= 60)
            System.out.println("D");
        else if (marks >= 50 )
            System.out.println("E");

       else
            System.out.println("F");
    }
}
